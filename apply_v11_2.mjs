import fs from "node:fs"
import path from "node:path"

const file = path.join(process.cwd(), "src/components/chat/ChatActionSheet.tsx")
const cssFile = path.join(process.cwd(), "src/chat-v11.css")

if (!fs.existsSync(file) || !fs.existsSync(cssFile)) {
  throw new Error("Не найдены файлы v11. Запусти скрипт из корня MEGANOTRPG.")
}

let source = fs.readFileSync(file, "utf8")
const oldBlock = "          <div className=\"dice-builder\">\n            <label>\n              Куб\n              <select\n                className=\"app-select\"\n                value={dieSides}\n                onChange={(event) => setDieSides(Number(event.target.value))}\n              >\n                {dice.map((sides) => (\n                  <option key={sides} value={sides}>\n                    d{sides}\n                  </option>\n                ))}\n              </select>\n            </label>\n\n            <label>\n              Кол-во\n              <input\n                className=\"app-input\"\n                type=\"number\"\n                min=\"1\"\n                max=\"20\"\n                value={dieCount}\n                onChange={(event) =>\n                  setDieCount(\n                    Math.max(1, Math.min(20, Number(event.target.value) || 1)),\n                  )\n                }\n              />\n            </label>\n\n            <label>\n              Модификатор\n              <input\n                className=\"app-input\"\n                type=\"number\"\n                min=\"-100\"\n                max=\"100\"\n                value={modifier}\n                onChange={(event) =>\n                  setModifier(\n                    Math.max(-100, Math.min(100, Number(event.target.value) || 0)),\n                  )\n                }\n              />\n            </label>\n          </div>"
const newBlock = "          <div className=\"dice-builder dice-builder--mobile\">\n            <label className=\"dice-select-field\">\n              Куб\n              <select\n                className=\"app-select\"\n                value={dieSides}\n                onChange={(event) => setDieSides(Number(event.target.value))}\n              >\n                {dice.map((sides) => (\n                  <option key={sides} value={sides}>\n                    d{sides}\n                  </option>\n                ))}\n              </select>\n            </label>\n\n            <div className=\"dice-step-field\">\n              <span className=\"dice-step-field__label\">Количество</span>\n              <div className=\"dice-stepper\">\n                <button\n                  type=\"button\"\n                  aria-label=\"Уменьшить количество кубиков\"\n                  onClick={() => setDieCount((value) => Math.max(1, value - 1))}\n                  disabled={dieCount <= 1}\n                >\n                  −\n                </button>\n                <strong>{dieCount}</strong>\n                <button\n                  type=\"button\"\n                  aria-label=\"Увеличить количество кубиков\"\n                  onClick={() => setDieCount((value) => Math.min(20, value + 1))}\n                  disabled={dieCount >= 20}\n                >\n                  +\n                </button>\n              </div>\n              <div className=\"dice-preset-row\">\n                {[1, 2, 3, 4, 5].map((value) => (\n                  <button\n                    type=\"button\"\n                    key={value}\n                    className={dieCount === value ? \"dice-preset dice-preset--active\" : \"dice-preset\"}\n                    onClick={() => setDieCount(value)}\n                  >\n                    {value}\n                  </button>\n                ))}\n              </div>\n            </div>\n\n            <div className=\"dice-step-field\">\n              <span className=\"dice-step-field__label\">Модификатор</span>\n              <div className=\"dice-stepper\">\n                <button\n                  type=\"button\"\n                  aria-label=\"Уменьшить модификатор\"\n                  onClick={() => setModifier((value) => Math.max(-100, value - 1))}\n                  disabled={modifier <= -100}\n                >\n                  −\n                </button>\n                <strong>{modifier > 0 ? `+${modifier}` : modifier}</strong>\n                <button\n                  type=\"button\"\n                  aria-label=\"Увеличить модификатор\"\n                  onClick={() => setModifier((value) => Math.min(100, value + 1))}\n                  disabled={modifier >= 100}\n                >\n                  +\n                </button>\n              </div>\n              <div className=\"dice-preset-row dice-preset-row--modifier\">\n                {[-5, -2, -1, 0, 1, 2, 5].map((value) => (\n                  <button\n                    type=\"button\"\n                    key={value}\n                    className={modifier === value ? \"dice-preset dice-preset--active\" : \"dice-preset\"}\n                    onClick={() => setModifier(value)}\n                  >\n                    {value > 0 ? `+${value}` : value}\n                  </button>\n                ))}\n              </div>\n            </div>\n          </div>"

if (!source.includes("dice-builder--mobile")) {
  if (!source.includes(oldBlock)) {
    throw new Error("Не удалось найти старый блок кубиков. Ничего вручную не меняй — пришли мне этот текст ошибки.")
  }
  source = source.replace(oldBlock, newBlock)
  fs.writeFileSync(file, source, "utf8")
}

let css = fs.readFileSync(cssFile, "utf8")
const cssAppend = "\n/* v11.2 mobile dice controls */\n.dice-builder--mobile{\n  grid-template-columns:1fr;\n  gap:10px;\n}\n.dice-select-field{\n  display:flex;\n  flex-direction:column;\n  gap:5px;\n}\n.dice-step-field{\n  display:flex;\n  flex-direction:column;\n  gap:6px;\n}\n.dice-step-field__label{\n  color:#8b8592;\n  font-size:9px;\n}\n.dice-stepper{\n  min-height:46px;\n  display:grid;\n  grid-template-columns:52px minmax(0,1fr) 52px;\n  gap:7px;\n  align-items:center;\n}\n.dice-stepper button{\n  min-height:46px;\n  border:1px solid #3a3147;\n  border-radius:12px;\n  background:#1a1620;\n  color:#ddd6fe;\n  font-size:25px;\n  line-height:1;\n  font-weight:500;\n}\n.dice-stepper button:disabled{\n  opacity:.3;\n}\n.dice-stepper strong{\n  min-height:46px;\n  border:1px solid #302b37;\n  border-radius:12px;\n  background:#101012;\n  display:grid;\n  place-items:center;\n  color:#f4f4f5;\n  font-size:16px;\n  font-variant-numeric:tabular-nums;\n}\n.dice-preset-row{\n  display:grid;\n  grid-template-columns:repeat(5,minmax(0,1fr));\n  gap:5px;\n}\n.dice-preset-row--modifier{\n  grid-template-columns:repeat(7,minmax(0,1fr));\n}\n.dice-preset{\n  min-height:32px;\n  padding:0 4px;\n  border:1px solid #2e2934;\n  border-radius:9px;\n  background:#121214;\n  color:#8d8794;\n  font-size:9px;\n  font-weight:700;\n}\n.dice-preset--active{\n  border-color:#5c4380;\n  background:#241a31;\n  color:#ddd6fe;\n}\n@media(max-width:390px){\n  .dice-builder--mobile{grid-template-columns:1fr}\n  .dice-stepper{grid-template-columns:48px minmax(0,1fr) 48px}\n  .dice-preset-row--modifier{gap:4px}\n  .dice-preset{min-height:30px;font-size:8px}\n}\n"

if (!css.includes("/* v11.2 mobile dice controls */")) {
  css += cssAppend
  fs.writeFileSync(cssFile, css, "utf8")
}

console.log("Готово: мобильный выбор кубиков исправлен.")
console.log("Теперь выполни: npm run build")
